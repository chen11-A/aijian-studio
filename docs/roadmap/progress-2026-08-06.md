# Aijian Studio 当前进度（2026-08-06）

## 结论

按“必须有实现、自动测试和验收证据才算完成”的严格口径，首个 8 周 Backlog 当前为 **17/43 完整完成（39.5%）**。部分完成按 50% 折算约为 **55.8%**。换算整个 48 周路线图，并考虑部分 W9–W16 能力被提前实现，当前合理估计为 **10%–11%**。

项目处于 **Sprint 3（W5–W6）早期**，不是实际日历已经消耗了 5–6 周，而是功能依赖走到了这一层。W1–W8 的阶段 0 尚未完成。

## 首个 8 周盘点

| 阶段  |  完整完成 | 部分完成 | 未开始 | 变化                    |
| ----- | --------: | -------: | -----: | ----------------------- |
| W1–W2 |      7/12 |        2 |      3 | 无变化                  |
| W3–W4 |      8/13 |        4 |      1 | F06 从未开始变为完成    |
| W5–W6 |       2/8 |        1 |      5 | M03 从收尾变为完成      |
| W7–W8 |      0/10 |        7 |      3 | 无变化                  |
| 合计  | **17/43** |   **14** | **12** | 比上次严格口径增加 2 项 |

本表沿用上一次 43 项审计口径，并只根据本次已有仓库证据上调 F06、M03，避免把提前出现的模型、界面或接口骨架重复计分。

## 本次确认完成

### M03 基础时间线与导出

- 不可变 trim、reorder、replace 和代理引用。
- 25fps 与 30000/1001 精确帧/音频采样映射。
- 1080×1920 MP4 开发证据导出、资源上限和单并发保护。
- 产品发布编码器因许可未批准而 fail-closed；不能计作正式发布链路。

### F06 Fake Provider 与故障注入

- 独立 Python 子进程、stdin/stdout NDJSON，本机不监听网络。
- SQLite 持久化 Fake Job；相同幂等键和请求 hash 只产生一个 Job。
- 提交前受控错误、提交前崩溃、提交后回包前崩溃。
- 重启、查询、幂等重试和子进程清理证据。
- 固定种子故障决策及 Windows/Linux CI lane。
- Q03 的六个生产 kill 点 ×100 种子仍未完成。

## 当前验证基线

- Python：518 tests passed；最近一次全量覆盖率门禁保持 line ≥90%、branch ≥83.5%。
- Electron Desktop：50 tests passed。
- Studio Web：56 tests passed。
- lint、format、mypy、TypeScript typecheck、build、OpenAPI drift、证据 SHA-256 全部通过。
- 生产 JavaScript 依赖审计：无已知漏洞。

这些是本地 Windows 验证结果；新增的双平台 CI lane 只有推送远端后才能形成 GitHub Actions 证据。本次未获授权推送，因此不把 CI 配置存在当成远端 CI 已通过。

## 下一关键目标

不是直接做真实付费 Provider，而是先完成 **Sprint 3 可安装 Walking Skeleton**：

1. 完成 UI01 的基础时间线交互和完整 Web/Electron E2E。
2. 完成 K01：2 万字小说 → Fake 剧本/分镜/画面/声音资产 → 时间线 → MP4，全程只用 UI/API，不手改数据库。
3. 完成 Q03：六个生产 kill 点各 100 个确定性种子，未知远程结果不自动重提。
4. 完成 Q02/Q04：Windows 安装包、中文用户名、长路径、Defender 和普通用户验收。
5. 由影片剪辑角色完成 C03 黄金纵切并记录 workaround、人工修复时间和无法发布镜头。

### 2026-08-10 UI01 时间线子项进展

基础时间线已经从纯 Python 领域模型推进为真实产品纵切：SQLite 不可变 Artifact 版本、
项目级 OpenAPI、Web transport、受限 Electron IPC、剪辑台交互和 Web/Electron 实机自动化均已通过。
真实浏览器完成一次 REV 1 → REV 2 裁剪，真实 Electron 再次证明持久化、Renderer 隔离、
控制台清洁和无整页横向溢出。验收见
[基础时间线工作台记录](../quality/phase0-timeline-workspace-acceptance.md)。

UI01 仍保持“部分完成”，因为统一 `web-e2e-skeleton` 尚未在一次运行中串联创建、导入、任务与
时间线。严格完成数仍为 17/43，不据此虚增总进度。下一步应先关闭这条统一 E2E，再进入 K01
的 2 万字 Fake 资产到 MP4 自动纵切。

完成整个 Sprint 3（不是只完成 K01 一项）后，48 周总进度才适合上调到约 **15%–17%**。

## 仍然阻塞完整产品的事项

- 真实 OpenAI/xAI/其他 Provider 的文本、图片、视频、TTS 资格和成本证据；会员网页权益不能代替 API。
- Markdown/DOCX 摄取、CAS、完整失效传播、远程预算/对账状态机。
- 正式 G2–G8 人工审批、权利证明和影视团队 held-out 验收。
- 剧本、导演分镜、角色连续性、声音和正式 Assembly Editor。
- Creator Beta、Studio Beta、GA 的安装、协作、服务端、发布和运维工作。

## 仓库状态口径

M03 与 F06 目前是本地分支成果，尚未推送或合并远端 `main`，因此属于“本地完成、未发布”。远端发布进度必须另行统计，不能与本地实现进度混为一谈。
